apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-collector.fullname" . }}
  labels:
    {{- include "kube-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-collector.selectorLabels" . | nindent 4 }}
